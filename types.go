package migrator

// Optional: define constants for tag keys or SQL types
const (
	TagPrimary       = "primary"
	TagAutoIncrement = "auto_increment"
	TagUnique        = "unique"
	TagNotNull       = "notnull"
)
